<style type="text/css">
	:root {
		--pad: 1em;
		--bordr: 5px;
		--grad: linear-gradient(-145deg, #69EACB 0%, #EACCF8 48%, #6654F1 100%);
		--blk: #212121;
	}
	body {
		color: var(--blk);
		padding: var(--pad);
		padding-bottom: calc(2*var(--pad));
		font-size: 16px;
	}
	* {
		font-family: 'Merriweather', serif;
	}
	h1, h2, h3, h4 {
		font-family: 'Lato', sans-serif;
	}
	h1 {
		text-align: center;
		font-size: 4rem;
	}
	hr {
		margin: 1.5rem 0;
	}
	.no-spacing {
		margin: 0;
		padding: 0;
	}
	.gradient {
		background-image: var(--grad);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
	}
	.visually-hidden {
		position: absolute !important;
		width: 1px !important;
		height: 1px !important;
		padding: 0 !important;
		margin: -1px !important;
		overflow: hidden !important;
		clip: rect(0, 0, 0, 0) !important;
		white-space: nowrap !important;
		border: 0 !important;
	}
	input {
		outline-offset: -2px;
		display: block;
		width: calc(100% - var(--pad) - var(--pad));
		padding: .375rem .75rem;
		font-size: inherit;
		font-weight: 400;
		line-height: 1.5;
		color: #212529;
		background-color: #fff;
		background-clip: padding-box;
		border: 1px solid #ced4da;
		-webkit-appearance: none;
		-moz-appearance: none;
		appearance: none;
		border-radius: .25rem;
		transition: border-color .15s ease-in-out,box-shadow .15s ease-in-out;
	}
	.horoscope {}
	.horoscope__title {}
	.horoscope__body {}

	.tab-set {
		display: flex;
		justify-content: center;
	}
	.tab-set__wrapper {
		display: flex;
		justify-content: space-evenly;
		flex-grow: 1;
		max-width: 350px;
		list-style: none;
	    padding-inline-start: 0;
	}
	.tab-set__item {
		font-family: 'Lato', sans-serif;
		flex: 1 0 50%;
		text-align: center;
		padding: var(--pad);
		cursor: pointer;
		/*border-radius: var(--pad);*/
		background: white;
		border: var(--bordr) solid;
		border-image-source: var(--grad);
  		border-width: calc(var(--bordr)/2);
		border-image-slice: 1;
		color: var(--blk);
	}
	/*.tab-set__item:first-of-type {
		border-top-right-radius: 0;
		border-bottom-right-radius: 0;
	}
	.tab-set__item:last-of-type {
		border-top-left-radius: 0;
		border-bottom-left-radius: 0;
	}*/
	.tab-set__item--selected {
		cursor: none;
		background: var(--blk);
		border: var(--bordr) solid var(--blk);
		color: white;
	}
</style>
